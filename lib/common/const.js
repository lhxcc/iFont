'use strict';

const path = require('path');

// 定义全局常量
module.exports = {
    ROOTPATH : path.join(__dirname, '../..'),     // 项目根目录
    PROJECT_NAME : 'ya-data',                     // 项目名称
    VERSION: 'v1.0.0',                            // 项目版本
    BUILDTIME: '20170106'                         // 项目BUILD日期
};
